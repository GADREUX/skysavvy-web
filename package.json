"use client"

import { IslandPattern } from "@/components/IslandPattern"

export default function WelcomePage() {
  return (
    <main style={{ position: "relative", minHeight: "100vh", overflow: "hidden" }}>
      <IslandPattern scale={900} />
      <div style={{
        position: "relative", zIndex: 1, minHeight: "100vh",
        display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center",
        padding: "2rem", textAlign: "center", color: "#fff",
      }}>
        <div style={{ display: "flex", alignItems: "center", gap: 10, marginBottom: 48 }}>
          <div style={{
            width: 32, height: 32, borderRadius: "50%",
            background: "rgba(255,255,255,0.2)", border: "1px solid rgba(255,255,255,0.3)",
          }} />
          <div style={{
            fontFamily: "var(--font-sans)", fontWeight: 900,
            fontSize: 20, letterSpacing: "-0.01em",
          }}>
            skysavvy
          </div>
        </div>

        <h1 style={{
          fontFamily: "var(--font-display)", fontSize: "clamp(2.5rem, 5vw, 4.5rem)",
          lineHeight: 1.05, color: "#fff", maxWidth: "16ch",
        }}>
          Track every fare quietly.
        </h1>

        <p style={{
          fontFamily: "var(--font-serif)", fontSize: 18,
          color: "rgba(255,255,255,0.85)", marginTop: 16, maxWidth: 420,
          lineHeight: 1.5, fontStyle: "italic",
        }}>
          Pin a route. We watch it in the background — pulling fares from
          multiple sources and notifying you when the price moves.
        </p>

        <button
          onClick={() => window.location.href = "/dashboard"}
          style={{
            marginTop: 32, background: "rgba(255,255,255,0.95)", color: "var(--ink)",
            fontFamily: "var(--font-sans)", fontWeight: 800, fontSize: 14,
            padding: "12px 28px", borderRadius: 8, letterSpacing: "0.04em",
            border: "none", cursor: "pointer",
            transition: "transform var(--t-fast) var(--ease)",
          }}
        >
          Enter dashboard
        </button>

        <div style={{
          position: "absolute", bottom: 24,
          fontFamily: "var(--font-sans)", fontSize: 11, fontWeight: 800,
          letterSpacing: "0.14em", textTransform: "uppercase",
          color: "rgba(255,255,255,0.5)",
        }}>
          Flights · Trends · Discovery
        </div>
      </div>
    </main>
  )
}
